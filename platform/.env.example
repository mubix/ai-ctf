# Copy to .env and edit. Required only for the `web` service.
# Generate a real secret with: python -c "import secrets; print(secrets.token_hex(32))"
SESSION_SECRET=replace-me-before-event-with-a-real-secret

# Only the web UI is published on the host. Ollama and decoy stay private.
CTF_WEB_PORT=18080
